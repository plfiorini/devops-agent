# Agent Instructions

## Runtime

- Run `.ts` files via `node --experimental-transform-types`. No `tsx`, `ts-node`, Babel, SWC, or esbuild.
- For React/Ink UI use `React.createElement`; no JSX/TSX.
- Dev, prod, and tests all use native Node TypeScript flags.
